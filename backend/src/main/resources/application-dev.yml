# Local development. Only the differences from application.yml belong here.
spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.amartya.portfolio: DEBUG
    org.hibernate.SQL: DEBUG
